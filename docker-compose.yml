version: '3.8'

services:
  streamflix:
    build: .
    container_name: streamflix_app
    restart: always
    ports:
      - "80:80"
    volumes:
      # Map the local 'data' folder to the container's data directory
      # This ensures playlist.json persists across restarts
      - ./data:/app/data
    environment:
      - NODE_ENV=production